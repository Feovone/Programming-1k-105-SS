#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <ctype.h>
#define ID 10
#define NAME 30
#define TEL 14
#define KURS 3
#define LEN 50

int main()
{
    struct student{
        int id[ID];
        char name[NAME];
        int tel[TEL];
        int kurs[KURS];
        int math;
        int phych;
    }s;
    FILE *ishodnik;
    FILE *resual;
    int j=
    for(int j=0;j<=ID;j++){
    for(int i=0;i<=ID;i++){
    while(s(j).id[]!=','){
    fread(s(j).id,1,1,ishodnik);
    }
    }

}
